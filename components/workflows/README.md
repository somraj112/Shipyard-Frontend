# Workflow Components

> Components for visualizing the Engineering Workflow State Machine

This directory will contain components that render workflow states and transitions:
- State machine visualizer (Backlog → Merge → Release)
- Pull request lifecycle tracker
- Kanban-style workflow board
