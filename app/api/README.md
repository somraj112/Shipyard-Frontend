# Next.js API Routes (BFF Layer)

> Backend-for-Frontend proxy routes

This directory will contain Next.js API routes that act as a proxy layer between the frontend and the Shipyard backend services. This pattern:

- Keeps API keys and tokens server-side
- Enables request aggregation across microservices
- Simplifies CORS handling
